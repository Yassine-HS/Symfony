{% extends 'base.html.twig' %}

{% block title %}RatingTutoriel index{% endblock %}

{% block body %}
    <h1>RatingTutoriel index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Rating</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rating_tutoriel in rating_tutoriels %}
            <tr>
                <td>{{ rating_tutoriel.id }}</td>
                <td>{{ rating_tutoriel.rating }}</td>
                <td>
                    <a href="{{ path('app_rating_tutoriel_show', {'id': rating_tutoriel.id}) }}">show</a>
                    <a href="{{ path('app_rating_tutoriel_edit', {'id': rating_tutoriel.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rating_tutoriel_new') }}">Create new</a>
{% endblock %}
