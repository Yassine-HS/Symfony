<form method="post" action="{{ path('app_category_delete', {'id_category': category.id_category}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id_category) }}">
    <button class="btn btn-primary">Delete</button>
</form>
