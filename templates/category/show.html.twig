

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_category</th>
                <td>{{ category.getIdCategory() }}</td>
            </tr>
            <tr>
                <th>Name_category</th>
                <td>{{ category.getNameCategory() }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'id_category': category.getIdCategory()}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
