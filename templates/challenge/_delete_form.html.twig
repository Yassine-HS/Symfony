<form method="post" action="{{ path('app_challenge_delete', {'id_challenge': challenge.id_challenge}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ challenge.id_challenge) }}">
    <button class="btn">Delete</button>
</form>
