<form method="post" action="{{ path('app_rating_delete', {'id_rating': rating.id_rating}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rating.id_rating) }}">
    <button class="btn">Delete</button>
</form>
