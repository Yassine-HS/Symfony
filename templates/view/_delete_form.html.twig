<form method="post" action="{{ path('app_view_delete', {'id_view': view.id_view}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ view.id_view) }}">
    <button class="btn">Delete</button>
</form>
