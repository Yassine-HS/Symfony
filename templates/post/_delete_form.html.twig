<form method="post" action="{{ path('app_post_delete', {'id_post': post.getIdPost}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.getIdPost) }}">
    <button class="btn">Delete</button>
</form>
